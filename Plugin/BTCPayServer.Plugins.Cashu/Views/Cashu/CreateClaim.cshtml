@model BTCPayServer.Plugins.Cashu.ViewModels.CashuClaimCreateViewModel
@{
    ViewData["Title"] = "Create Cashu Claim Link";
}

<div class="container py-4">
    <div class="row justify-content-center">
        <div class="col-lg-7 col-md-9">
            <div class="card shadow-sm border-0">
                <div class="card-body">
                    <h2 class="mb-4 text-center">Create Cashu Claim Link</h2>
                    <form method="post">
                        <div class="mb-3">
                            <label class="form-label fw-bold" for="AmountSats">Amount (sats)</label>
                            <input asp-for="AmountSats" class="form-control" min="1" />
                            <span asp-validation-for="AmountSats" class="text-danger"></span>
                        </div>
                        <div class="mb-3">
                            <label class="form-label fw-bold" for="ExpiryMinutes">Expiry (minutes, optional)</label>
                            <input asp-for="ExpiryMinutes" class="form-control" min="0" placeholder="e.g. 60 for 1 hour" />
                            <span asp-validation-for="ExpiryMinutes" class="text-danger"></span>
                        </div>
                        <div class="d-flex justify-content-end">
                            <button type="submit" class="btn btn-primary">Create Link</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

<partial name="_ValidationScriptsPartial" />

